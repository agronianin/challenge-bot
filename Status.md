# Status

## Назначение файла

Этот файл нужен для восстановления рабочего контекста по проекту.
Если память о прошлой работе потеряна, нужно:

1. прочитать `Promt.md`;
2. прочитать этот файл;
3. затем открыть `README.md`.

После этого должно быть понятно:

- на каком этапе находится проект;
- что уже сделано;
- что еще не сделано;
- какой следующий шаг нужно выполнять.

## Текущее состояние проекта

Проект находится на раннем инфраструктурном этапе.

Что уже есть:

- Spring Boot приложение;
- базовая интеграция с Telegram Bot API;
- PostgreSQL через Spring Data JPA;
- Liquibase changelog;
- Dockerfile;
- Docker Compose;
- `.env.example`;
- README на русском;
- отделенный процесс миграций через отдельный compose-сервис `migrate`.
- корневой Java package `su.msk.nlx2.challengebot`;
- добавлен Maven Wrapper с Maven 3.9.14 для сборки под Java 25 без Maven/Guice warning из старого Maven 3.9.11;
- `logback-spring.xml` с двумя ротационными файлами логов.
- runtime-конфиги бота выносятся в bind-mounted каталог `./config`, доступный с сервера без входа в контейнер;
- контейнер `bot` по умолчанию публикует JDWP debug-порт `5005` для удаленной отладки;
- в модели `Exercise` уже добавлены поля для статичной части повторений.
- в модели `Exercise` добавлено поле `repsUnit` для единицы измерения `REPS/SEC`;
- таблицы БД переведены на singular naming; таблица пользователя называется `tg_user`;
- у `chat` primary key теперь `tg_chat_id`, отдельного surrogate `id` больше нет;
- Liquibase changelog схлопнут под чистую базу и сразу создает актуальную схему без переходных rename/migration changeset;
- у `exercise_type` primary key теперь `name`, отдельного `id` нет;
- в `program` добавлено поле `restDayFrequency` с дефолтом `0`;
- при создании challenge администратор задает `restDayFrequency`;
- Java-сущность пользователя переименована в `TgUser`, таблица БД остается `tg_user`;
- в модели `TgUser` сохраненное поле локали называется `localeCode`, а runtime `Locale` хранится в transient-поле `locale`;
- в модели `TgUser` добавлено поле `maxPullUps`;
- добавлена таблица `user_reminder` для хранения нескольких личных времен напоминаний пользователя;
- при первом личном диалоге бот обязательно запрашивает `maxPullUps`;
- в личке доступны действия: изменить `maxPullUps`, добавить reminder, показать reminders, удалить reminder;
- добавлена базовая инфраструктура локализации: `LocalizationConfig`, `LocalizationProperties`, `BotMessages`, `messages_ru/en.properties`;
- `UpdateHandler` переведен на routing через `MessageHandlerProvider`;
- `UpdateHandler` ужат до верхнеуровневого routing и делегирует в `BotRequestContextResolver`, `PrivateChatUpdateHandler`, `CallbackQueryUpdateHandler`;
- `BotUserContext` удален: обработчики получают `TgUser`, роль берется из `user.isAdmin()`, tg id из `user.getTgId()`, локаль из `user.getLocale()`;
- текстовые шаги диалогов вынесены в отдельные handler-классы;
- обработчики шагов диалогов разнесены по подпакетам `service.bot.message.admin` и `service.bot.message.user`;
- импорт CSV упражнений обрабатывается через `ConversationStep.AWAIT_EXERCISE_CSV` и отдельный `MessageHandler`, без специальной ветки в `PrivateChatUpdateHandler`;
- пользовательское личное меню переведено на `InlineKeyboardMarkup` + `callback_data`, без сравнения локализованного текста;
- callback-действия пользовательского меню маршрутизируются через `UserMenuActionHandlerProvider` и отдельные handler-классы;
- административное меню переведено на `InlineKeyboardMarkup` + `callback_data`, без сравнения локализованного текста;
- callback-действия административного меню маршрутизируются через `AdminMenuActionHandlerProvider` и отдельные handler-классы;
- стартовые admin menu-действия (добавление админа, старт challenge, импорт CSV) перенесены из `PrivateChatUpdateHandler` в соответствующие admin handler-классы;
- пользовательский и административный flow объединены в `ConversationService` / `ConversationSession` / `ConversationStep`;
- структура пакетов приведена к правилу: сервисы в `service`, модели в `model`, enum в `model.type`;
- корневой пакет `su.msk.nlx2.challengebot.bot` полностью убран;
- есть административная панель в личном чате для пользователей с `role = ADMIN`;
- административные действия вынесены во вложенное меню `Администрирование`;
- роль пользователя в коде хранится как enum `UserRole`, а в БД как строка `USER/ADMIN`;
- статус challenge в коде хранится как enum `ProgramStatus`, а в БД как строка `ACTIVE/SCHEDULED/PAUSED`;
- есть добавление нового администратора через `requestUsers`;
- есть пошаговое создание challenge через `requestChat` и последовательный ввод параметров;
- при создании challenge timezone выбирается готовыми кнопками с возможностью ручного ввода любого корректного `ZoneId`;
- есть список challenge для админа с возможностью поставить challenge на паузу и удалить его;
- есть импорт метаданных упражнений из CSV через личный чат администратора;
- есть таблица `program_participant` для участия пользователя в challenge;
- есть callback-кнопка `Участвовать` в стартовом посте challenge и в посте дня;
- повторное нажатие `Участвовать` активным участником не отправляет личное задание повторно, а отправляет ошибку вступления;
- есть кнопка `Сдаюсь` в личном меню активного участника, она завершает все активные участия пользователя через `program_participant.active = false`;
- есть scheduler, который публикует запланированный день challenge в групповой чат;
- scheduler учитывает `restDayFrequency`: в выходной день публикует сообщение о выходном и не подбирает упражнения;
- общий пост дня отправляется без повторений, а личная версия участникам отправляется с количеством повторений;
- дата старта challenge больше не вводится вручную, challenge стартует с завтрашнего дня;
- сообщения общего поста сохраняются в `program_day_message`;
- администратор может перевыложить последний опубликованный день challenge;
- если `video_path` пустой или неверный, бот публикует текст задания вместо видео;
- на каждом шаге админского сценария есть `Отмена`.
- любой пользователь, написавший боту сообщение, автоматически заносится или обновляется в таблице `tg_user`.

Что еще не готово:

- полный первый запуск на реальном сервере не подтвержден;
- проверка реального `docker compose up -d` из текущего агентского окружения не выполнена, потому что здесь нет доступного Docker daemon;
- продуктовая логика challenge в основном еще не реализована.

## Ключевые принятые решения

- Секреты хранятся только в локальном `.env`.
- `.env.example` хранится в репозитории без секретов.
- Liquibase не встроен в runtime приложения.
- Миграции запускаются вручную отдельной командой.
- новые изменения Liquibase разбиваются на отдельные changeset.
- локализация должна использовать `MessageSource` + `messages_*.properties`, а язык пользователя должен храниться в `tg_user.locale`;
- в коде сохраненный язык называется `TgUser.localeCode`, а текущая разрешенная локаль запроса хранится в `TgUser.locale`;
- текущие файлы локализации: `messages_ru.properties` и `messages_en.properties`;
- если локаль Telegram не поддерживается, приложение должно использовать `ru`;
- редактируемые runtime-конфиги должны жить в локальном `./config`, который создается отдельной инициализацией из образа и монтируется в контейнер бота;
- текстовые шаги диалогов должны обрабатываться отдельными handler-классами через `MessageHandlerProvider`;
- в `PrivateChatUpdateHandler` нельзя добавлять отдельные ветки под конкретные `ConversationStep`; нестандартные Telegram message нужно передавать в handler через `MessageHandlerContext`;
- пользователь и администратор должны идти по одному общему conversation-flow, а админские возможности должны быть дополнительными ветками внутри него;
- сервисы должны лежать только в `service`, модели только в `model`, enum только в `model.type`;
- README является текущей инструкцией по запуску и обновлению.
- таблицы БД должны называться в единственном числе;
- `tg_user` используется вместо `user`, потому что `user` конфликтует с PostgreSQL;
- `chat.tg_chat_id` используется как primary key без отдельного `id`.

### Этап 0. Локализация

Статус: `[x]`

План:

- [x] добавить хранение `tg_user.locale`;
- [x] добавить `MessageSource` и конфиг локализации;
- [x] добавить сервис `BotMessages`;
- [x] создать `messages_ru.properties` и `messages_en.properties`;
- [x] вынести тексты `UpdateHandler` на ключи локализации;
- [x] вынести подписи кнопок из factory-классов на ключи локализации;
- [x] добавить изменение языка пользователем через бота.
- `Promt.md` является источником продуктовых требований и архитектурных решений.

## Этапы

### Этап 1. Поднять самый простой deploy на сервере

Статус: `[~]`

Цель:

- поднять PostgreSQL;
- поднять приложение;
- подключить Telegram;
- убедиться, что первый запуск воспроизводим.

Что уже сделано:

- [x] удалены захардкоженные секреты из runtime-конфига;
- [x] введен `.env.example`;
- [x] `.env` оставлен только локальным и исключен из Git;
- [x] Docker Compose приведен к разделению `postgres`, `bot`, `migrate`;
- [x] Liquibase вынесен из приложения;
- [x] в README описан ручной порядок миграций;
- [x] логирование направлено в доступный путь;
- [x] startup Telegram сделан безопасным при пустом токене.

Что еще осталось:

- [ ] один раз проверить реальный запуск PostgreSQL;
- [ ] один раз проверить ручной запуск миграций;
- [ ] один раз проверить запуск бота после миграций;
- [ ] убедиться, что приложение реально подключается к БД на сервере;
- [ ] убедиться, что бот реально выходит в online с валидным токеном.

Текущий blocker:

- из текущего окружения нельзя проверить Docker runtime, потому что Docker daemon недоступен.

Следующее практическое действие:

- выполнить первый реальный запуск на сервере или на локальной машине с рабочим Docker.

### Этап 2. Проверить обновление на новую версию

Статус: `[ ]`

Цель:

- убедиться, что новую версию можно выкатывать без потери данных и без лишних ручных действий.

План:

- [ ] обновить код на сервере;
- [ ] отдельно применить миграции;
- [ ] пересобрать и поднять только `bot`;
- [ ] проверить, что PostgreSQL не пострадал;
- [ ] проверить, что бот продолжил работать.

### Этап 3. Загрузить упражнения и подготовить CSV

Статус: `[~]`

План:

- [ ] определить место хранения видео;
- [ ] подготовить несколько видео;
- [x] определить окончательный CSV-формат;
- [x] реализовать импорт метаданных упражнений из CSV через админский диалог;
- [x] добавить пример CSV в репозиторий;
- [x] собрать уникальные упражнения из старых текстовых challenge в `examples/exercises-from-training-texts.csv`;
- [x] добавить seed стандартных типов упражнений через Liquibase, включая `Растяжка`;
- [x] перевести связь упражнений и типов на many-to-many через `exercise_type_link`;
- [ ] загрузить упражнения в БД.

### Этап 4. Запуск challenge из чата и публикация одного дня

Статус: `[~]`

План:

- [x] реализовать запуск challenge из административной панели в личке;
- [x] убрать ручной ввод даты старта и автоматически ставить завтрашний день;
- [x] показать администратору список существующих challenge;
- [x] дать возможность поставить challenge на паузу;
- [x] дать возможность перевыложить последний опубликованный день;
- [x] дать возможность удалить challenge;
- [x] создать запись challenge в БД;
- [x] заранее создать `ProgramDay` для всех дней;
- [x] создать первый день с упражнениями;
- [x] выбрать упражнения;
- [x] отправить в выбранный чат подтверждение создания challenge;
- [x] отправить полноценный стартовый пост challenge в чат;
- [x] сохранить факт публикации дня;
- [x] сохранять `message_id` сообщений общего поста;
- [x] защититься от повторной публикации.

### Этап 5. Участие пользователя

Статус: `[~]`

План:

- [x] добавить модель участия;
- [x] добавить кнопку вступления;
- [x] сохранять участие;
- [x] исключить дубли;
- [x] временно ограничить пользователя одним активным challenge;
- [x] хранить вычисленный флаг активного участия в `TgUser.activeParticipant` и убрать `PrivateChatState`;
- [x] добавить кнопку выхода из challenge;
- [ ] добавить пользовательский просмотр своих активных challenge.

### Этап 6. Личное сообщение участнику

Статус: `[~]`

План:

- [x] после вступления отправлять текущее задание в личку;
- [x] не принимать участие без первичной настройки `maxPullUps`, чтобы пользователь сначала открыл личный диалог с ботом;
- [ ] логировать ошибки отправки.

### Этап 7. Отметка выполнения дня

Статус: `[ ]`

План:

- [ ] добавить кнопку `готово`;
- [ ] сохранять completion;
- [ ] запретить дубли;
- [ ] хранить время отметки.

### Этап 8. Персональные данные и коэффициент участника

Статус: `[~]`

План:

- [x] определить первый личный параметр `maxPullUps`;
- [x] добавить хранение `maxPullUps` в модели пользователя;
- [x] добавить хранение нескольких времен reminders в `user_reminder`;
- [x] реализовать ввод этих данных через бота;
- [x] вычислять временный MVP `user_multiplier` на основе `maxPullUps`;
- [ ] уточнить финальную формулу `user_multiplier`.

### Этап 9. Расчет нагрузки

Статус: `[~]`

План:

- [ ] реализовать `exercise_multiplier`;
- [x] реализовать простой `day_multiplier` через рост по дням;
- [x] реализовать итоговый MVP-расчет;
- [x] учитывать единицу измерения `reps_unit` при выводе личного задания;
- [x] учесть `static_reps` как фиксированную добавку к коэффициентной части;
- [x] учесть режим `is_static_reps = true`, когда коэффициенты не применяются;
- [x] всегда округлять вверх;
- [x] сохранять базовый snapshot результата в `day_exercise.reps`;
- [ ] сохранять персональный snapshot результата для каждого участника.

### Этап 10. Автоматический daily workflow

Статус: `[~]`

План:

- [x] добавить scheduler;
- [x] публиковать дни автоматически;
- [x] исключить дубли при рестартах через статус `ProgramDay`;
- [x] при пустом или неверном `video_path` отправлять текст задания вместо видео;
- [ ] обработать ошибки отправки так, чтобы частичная публикация не приводила к неконсистентному состоянию.

### Этап 11. Напоминания

Статус: `[~]`

План:

- [x] хранить настройки reminders;
- [x] дать пользователю настроить reminders в личке;
- [ ] отправлять reminders активным участникам;
- [ ] не слать reminders тем, кто уже выполнил день.

### Этап 12. Статистика

Статус: `[ ]`

План:

- [ ] статистика текущего дня;
- [ ] список участников;
- [ ] личный прогресс;
- [ ] leaderboard.

## Что нужно помнить при дальнейшей работе

- Перед изменением поведения нужно обновлять `Promt.md`.
- После выполнения любого заметного шага нужно обновлять этот файл.
- Перед закрытием инфраструктурных задач нужно проверять их реальным запуском, а не только чтением кода.
- Не нужно смешивать инфраструктурные изменения и продуктовую логику в один шаг.

## Следующий шаг

Следующий правильный шаг сейчас:

1. выполнить первый реальный запуск на сервере;
2. посмотреть результат команд;
3. только после этого окончательно закрыть Этап 1.
